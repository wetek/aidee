### Purpose:
Install the Langfuse Python SDK into the assistant container image so assistants emit observability traces.

### Apply:
Rebuilds assistant image and reconciles running assistants.

### Validation:
Tests and repository validation pass.