# Infrastructure declarations

This directory will record the host configuration that the controller must reproduce or inspect, including:

- Assistant container names and ports.
- Dashboard hostnames.
- Storage locations and limits.
- Platform release assignments.
- Backup destinations without credentials.

The active assistant inventory remains in the generated private fleet registry.

